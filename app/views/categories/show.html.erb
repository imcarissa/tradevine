<h1><%= @category.name %></h1> - <%= pluralize(@category.bottles.count, "bottles") %>

<ul>
    <% @category.bottles.each do |b| %>
      <li>
      <%= link_to b.wine_name, bottle_path(b) %>
      </li>
    <% end  %>
</ul>